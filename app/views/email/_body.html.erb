<div>
	<h3> <%= email.object %> </h3>
	<p> <%= email.body %> </p>

	<%= link_to "Supprimer", email_path(@email.id), method: :delete, remote: true, class: "btn btn-danger" %>
	<button class="btn btn-secondary">Marquer comme non-lu</button>
</div>